/**
 * My-Restaurant · Search & SEO (Brief 09 §2). The v1 Search & SEO sub-tab moves
 * here unchanged — re-exported from its original home so no v1 content or test
 * is lost, only re-homed under the My Restaurant bucket.
 */
export { default } from '../SearchSEO';
