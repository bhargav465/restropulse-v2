name: Deploy Production

on:
  workflow_dispatch:
    inputs:
      ref:
        description: Git ref to promote (staging tag or commit SHA)
        required: true
        type: string
      services:
        description: Services to deploy (comma-separated or "all")
        required: false
        default: all
        type: string

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  build-web:
    name: Build Web
    if: contains(inputs.services, 'web') || inputs.services == 'all'
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Setup Node
        uses: ./.github/actions/setup-node

      - name: Login to Azure
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Fetch sensitive web vars from Key Vault
        run: |
          MAPS_KEY=$(az keyvault secret show \
            --vault-name restropulse-prod-kv \
            --name PROD-WEB-GOOGLE-MAPS-API-KEY \
            --query "value" -o tsv)
          echo "::add-mask::$MAPS_KEY"
          echo "VITE_GOOGLE_MAPS_API_KEY=$MAPS_KEY" >> $GITHUB_ENV

          APPINSIGHTS=$(az keyvault secret show \
            --vault-name restropulse-prod-kv \
            --name PROD-WEB-APPINSIGHTS-CONNECTION-STRING \
            --query "value" -o tsv)
          echo "::add-mask::$APPINSIGHTS"
          echo "VITE_APPINSIGHTS_CONNECTION_STRING=$APPINSIGHTS" >> $GITHUB_ENV

      - name: Build web
        run: npx turbo build --filter=@restropulse/web
        env:
          NODE_ENV: production
          VITE_API_URL: ${{ vars.PRODUCTION_API_URL }}/api
          VITE_APP_URL: ${{ vars.PRODUCTION_WEB_URL }}
          VITE_FIREBASE_API_KEY: ${{ vars.VITE_FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ vars.VITE_FIREBASE_AUTH_DOMAIN }}
          VITE_FIREBASE_PROJECT_ID: ${{ vars.VITE_FIREBASE_PROJECT_ID }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ vars.VITE_FIREBASE_STORAGE_BUCKET }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.VITE_FIREBASE_MESSAGING_SENDER_ID }}
          VITE_FIREBASE_APP_ID: ${{ vars.VITE_FIREBASE_APP_ID }}
          VITE_RAZORPAY_KEY_ID: ${{ vars.VITE_RAZORPAY_KEY_ID }}
          VITE_TELEMETRY_SAMPLE_RATE: ${{ vars.VITE_TELEMETRY_SAMPLE_RATE }}
          # VITE_GOOGLE_MAPS_API_KEY and VITE_APPINSIGHTS_CONNECTION_STRING
          # are injected via $GITHUB_ENV by the KV fetch step above

      - name: Upload web artifact
        uses: actions/upload-artifact@v7
        with:
          name: web-dist
          path: apps/web/dist/
          retention-days: 3

  deploy-web-production:
    name: Deploy Web (Production)
    needs: build-web
    if: contains(inputs.services, 'web') || inputs.services == 'all'
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download web artifact
        uses: actions/download-artifact@v8
        with:
          name: web-dist
          path: web-dist/

      - name: Login to Azure
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Get SWA deployment token
        id: swa_token
        run: |
          token=$(az staticwebapp secrets list \
            --name ${{ vars.AZURE_SWA_NAME }} \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }} \
            --query "properties.apiKey" -o tsv)
          echo "::add-mask::$token"
          echo "token=$token" >> $GITHUB_OUTPUT

      - name: Deploy to Azure Static Web Apps (production)
        uses: azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ steps.swa_token.outputs.token }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          skip_app_build: true
          app_location: web-dist/

  swap-api-production:
    name: Swap API + Workers to Production
    # Wait for web to deploy first so both services update together. If web
    # was not in this deployment (deploy-web-production skipped), always() lets
    # this job proceed anyway; !failure() blocks the swap if web deploy failed.
    needs: [deploy-web-production]
    if: >-
      always() &&
      !failure() &&
      !cancelled() &&
      (contains(inputs.services, 'api') ||
       contains(inputs.services, 'publisher') ||
       contains(inputs.services, 'content-engine') ||
       inputs.services == 'all')
    runs-on: ubuntu-latest
    environment: production
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Login to Azure
        uses: azure/login@v3
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      # All settings are slot-sticky so production keeps its own config
      # (NODE_ENV, DB, secrets) across slot swaps. WEBSITE_RUN_FROM_PACKAGE=1
      # mounts the zip as a read-only filesystem, bypassing Oryx entirely.
      - name: Configure production for zip-mount deploy
        run: |
          az webapp config appsettings set \
            --name ${{ vars.AZURE_WEBAPP_NAME_PROD }} \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }} \
            --slot-settings \
              NODE_ENV=production \
              CORS_ORIGIN="${{ vars.PRODUCTION_WEB_URL }}" \
              WEBSITE_RUN_FROM_PACKAGE=1 \
              WEBSITE_NODE_DEFAULT_VERSION="~22" \
              SCM_DO_BUILD_DURING_DEPLOYMENT=false

      - name: Swap staging slot to production
        run: |
          az webapp deployment slot swap \
            --name ${{ vars.AZURE_WEBAPP_NAME_PROD }} \
            --resource-group ${{ vars.AZURE_RESOURCE_GROUP }} \
            --slot staging \
            --target-slot production

  smoke-test-api:
    name: Smoke Test API
    needs: swap-api-production
    if: needs.swap-api-production.result == 'success'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
          sparse-checkout: scripts/smoke-test.sh

      - name: Smoke test API
        run: bash scripts/smoke-test.sh api "${{ vars.PRODUCTION_API_URL }}"

  smoke-test-web:
    name: Smoke Test Web
    needs: deploy-web-production
    if: needs.deploy-web-production.result == 'success'
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
          sparse-checkout: scripts/smoke-test.sh

      - name: Smoke test Web
        run: bash scripts/smoke-test.sh web "${{ vars.PRODUCTION_WEB_URL }}"

  tag-production:
    name: Tag Production
    needs: [smoke-test-api, smoke-test-web]
    if: always() && !failure() && !cancelled()
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}

      - name: Create production tag
        run: |
          TAG="production/$(date -u +'%Y-%m-%d-%H%M%S')"
          git tag "$TAG"
          git push origin "$TAG"
